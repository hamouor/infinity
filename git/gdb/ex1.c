#include<stdio.h>
#include<string.h>
#include<stdlib.h>

int main()

{
/*char* Strdup(const char* str)*/
	char str[] = "abcdef";
	size_t len = 0;
	char* dup = NULL;
	while(strlen(str)>0)
	{
		printf("kmt");
	}

	return 0;
}
